# ---- web / app ----
fastapi==0.121.0
uvicorn==0.38.0
Jinja2==3.1.6
python-multipart
pydantic_settings==2.14.1
watchfiles==1.1.1
httpx==0.28.1

# ---- agent graph ----
langchain_core==1.4.0
langchain_ollama==1.1.0
langgraph==1.2.0

# ---- scraping stack ----
beautifulsoup4==4.14.2
goose3==3.1.21
newspaper3k==0.2.8
playwright==1.57.0
readability-lxml==0.8.4.1
trafilatura==2.0.0
yt-dlp==2026.7.4

# ---- document ingestion (uploads) ----
pymupdf==1.26.3            # PDF text layer
python-docx==1.2.0         # .docx extraction
Pillow==11.3.0             # PDF page rendering for scanned pages
numpy==2.3.3               # optional: ~50x faster dense retrieval, falls back to pure Python
PyYAML==6.0.2              # optional: external data-classification rules
networkx==3.4.2            # flowchart_generator: layered graph layout, pure Python
python-docx
pymupdf
# ---- optional ----
# spacy + en_core_web_sm improve keyword/entity enrichment.
# Without them, enrichment falls back to heuristics — nothing breaks.
#   pip install spacy && python -m spacy download en_core_web_sm
